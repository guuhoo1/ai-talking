<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aitalking.mapper.ChatMessageMapper">
    <select id="selectBySessionId" resultType="com.aitalking.model.ChatMessage">
        SELECT * FROM chat_message WHERE session_id = #{sessionId} ORDER BY create_time ASC
    </select>

    <insert id="insert" parameterType="com.aitalking.model.ChatMessage">
        INSERT INTO chat_message (session_id, role, content, create_time) VALUES (#{sessionId}, #{role}, #{content}, NOW())
    </insert>
</mapper>